import { View, Text, Image } from 'react-native'
import React from 'react'
import { Icon } from 'react-native-elements'

export default function AnimatedIconCrypto({src}) {
  return (
    <Image source={src}/>
  )
}